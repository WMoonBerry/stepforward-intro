# 2026-09-10

## 合并 index.html 内容到 stepforward-proposal.html（产品介绍网站优化）

目标：以 stepforward-proposal.html 为基底，保留其暖米色 + 陶土橙(#c98b6b)/紫灰(#8b6f8e)/青绿(#6b8e8b) 配色，吸收 index.html 的内容。

已完成：
- **Hero 融合**：保留 proposal 的 badge/h1「一步一步，找回对生活的掌控感」/subtitle/hero-meta/quote，新增 index 的 hook「你不是懒——你只是需要先被接住」、三个 hero-point、手机 mockup（assets/A01.jpg）、「开始第一步 →」直达 Netlify 按钮。改为 `.hero-grid` 两栏布局，hero container 放宽到 1080px。
- **Core Journey**：新增 `#journey` 五步流程（拆解/夸夸/情绪陪伴/日记/晚安），带手机 mockup（B02/C02/D01/E02/F07）、箭头连接线，配色全部映射到 proposal 变量。
- **Product Decisions**：新增 `#decisions`（不做打卡积分 / 不做自主情绪闭环 Agent），插在「社会价值」之前。
- **第一轮内测**：新增 `#ux`（4 项 stats + 3 条关键发现 + 4 条用户原话 + 局限说明）。
- **迭代历程**：新增 `#iteration`（v1.0 → v2.2.8 时间轴）。
- **CTA**：保留 proposal 文案「世界很大，慢慢来」，加上 index 的体验/GitHub 按钮与 cta-meta。

顺带修复（原文件遗留问题）：
- AI 人格设计段落里 `<section id="roles">` 被错误嵌套在另一个 `<section>` 内 → 拆成独立 section，并把「绝对禁止的行为」移回 AI 人格设计 section。
- role-card / role-insight 引用了 proposal 中未定义的 `--shadow / --accent-light / --ink-soft / --accent2-light` → 在 :root 补齐。
- `<h3><WenYM</h3>` 标签破损 → 修正。
- mermaid.min.js 引用的 `./_shared/` 目录不存在 → 加 `typeof mermaid !== 'undefined'` 守卫，避免报错。

新增 CSS 变量：`--ink-soft / --accent-light / --accent2-light / --accent3-light / --shadow / --shadow-strong`。
新增 JS：IntersectionObserver 滚动淡入（.fade-in）。

最终章节顺序：Hero → 问题背景 → 产品定位 → 完整使用流程 → 核心差异化 → AI 人格设计 → AI 角色 → 理论依据 → 产品决策 → 社会价值 → 第一轮内测 → 迭代历程 → 创作故事 → 关于作者 → CTA。

注意：index.html 用 `assets\xxx.jpg` 反斜杠路径，合并时统一改为正斜杠。

## 按用户体验报告 v1.1 更新 index.html 的内测/迭代章节

数据源：`D:\MXM\Git_Hub_portfolio\deploy\Package_WYM-(新作品集)\03-用户体验报告\用户体验报告-作品集-温玥美.html`
（StepForward 轻量用户体验研究报告 v1.1，问卷 N=5 + 访谈/私信补充，2026-09）

注意：此时 stepforward-proposal.html 已被用户删除/合并进 index.html（index.html 现为 98KB 主页面，带 data-page-node-id 属性）。

更新内容（index.html `#ux` + `#iteration`）：
- 章节名「第一轮内测」→「第一轮用户体验研究」
- 数据卡：5 位（3 目标 + 2 非典型）／推荐意愿 9.3 → **8.8/10**／问题定位 5 项 → **9 项（6×P1 + 3×P2）**／"被接住" 100% → **4/5**
- 关键发现重写为 4 条：被接住感 4/5（含 1 位反例"像套话"）／5/5 秒懂走完全流程／9 项问题无 P0 且全在可容忍区间／拆解"够小"3/5 但时长粒度有波动
- 新增「用户反馈 → 我们的迭代动作」表格（5 行，含验证结果）
- 新增「意外发现」3 卡：吉祥物诉求／低门槛心理支持入口／音效与到点提醒
- 用户原话换成报告原文（华子、心心、ym、灰灰），并保留 1 条批评性原话体现诚实
- 局限说明补充：ym 与开发者关系亲近、窦窦为非目标用户、4 条待验证假设
- 迭代历程：标题 v2.2.x → v2.3；新增「v2.3 · 规划中」节点（planned 空心圆点样式）；v2.2.8 补「12 项仿真通过」
- 导航新增「内测反馈 → #ux」

新增 CSS：`.ux-sub-title / .resp-table-wrap / .resp-table / .surprise-grid / .surprise-card / .surprise-tag / .iter-item.planned`

经验：报告与落地页的数据口径必须对齐，报告是唯一事实源；数字（如推荐意愿）随样本从 3 → 5 会从 9.3 降到 8.8，不要沿用旧值。
